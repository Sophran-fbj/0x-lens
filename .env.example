# Optional: your own Ethereum mainnet RPC endpoint (e.g. Alchemy/Infura free tier).
# If unset, viem's default public RPC is used. Phase 2 also derives the
# host_permissions entry from this URL.
# VITE_RPC_URL=https://eth-mainnet.g.alchemy.io/v2/YOUR_KEY
